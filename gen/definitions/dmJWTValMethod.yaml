name: DmJWTValMethod
model_only: true
attributes:
- name: decrypt
  type: Bool
  default: false
  description: Decrypt the encrypted JWT
- name: verify
  type: Bool
  default: false
  description: Verify the signed JWT
- name: customized
  type: Bool
  default: false
  description: Use the custom processing mechanism to validate the JWT
