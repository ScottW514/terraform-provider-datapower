name: DmHTTPClientServerVersion
model_only: true
attributes:
- name: Front
  type: String
  enum:
  - HTTP/1.0
  - HTTP/1.1
  default: HTTP/1.1
  description: Select the HTTP version to use on the client-side connection. The default
    is HTTP 1.1.
- name: Back
  type: String
  enum:
  - HTTP/1.0
  - HTTP/1.1
  default: HTTP/1.1
  description: Select the HTTP version to use on the server-side connection. The default
    is HTTP 1.1.
