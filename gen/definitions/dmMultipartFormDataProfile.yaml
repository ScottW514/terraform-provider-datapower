name: DmMultipartFormDataProfile
model_only: true
attributes:
- name: MaxParts
  type: Int64
  default: 4
  description: Maximum number of parts allowed. Defaults to 4.
- name: MaxSizePerPart
  type: Int64
  default: 5000000
  description: Maximum size allowed for any one part. Defaults to 5000000.
- name: MaxSize
  type: Int64
  default: 5000000
  description: Maximum size allowed for all parts combined. Defaults to 5000000.
- name: ContentTypeRestricted
  type: Bool
  default: false
  description: If set, this forces the individual form-data content types to be matched
    against the general list of request acceptable content-type expressions.
