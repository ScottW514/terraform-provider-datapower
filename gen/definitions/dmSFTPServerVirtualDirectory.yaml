name: DmSFTPServerVirtualDirectory
model_only: true
list_item: true
attributes:
- name: VirtualPath
  type: String
  description: Specify the directory in the virtual file system of the SFTP server.
  string_patterns:
  - ^/[^/]+(/[^/]+)*$
